{
  "name": "b64id",
  "version": "1.0.0",
  "description": "url-safe 128-bit UUID strings expressed in base-64 encoding",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover _mocha && npm run check-coverage",
    "check-coverage": "istanbul check-coverage --statements 100 --branches 100 --lines 100 --functions 100"
  },
  "keywords": [
    "base64",
    "base64-url",
    "uuid",
    "guid"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:monteslu/b64id.git"
  },
  "author": "Luis Montes",
  "license": "ISC",
  "dependencies": {
    "base64-url": "^2.2.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.0.2"
  }
}
